<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body class="w-full h-screen bg-zinc-100">
    <!-- navbar -->
    <nav class="bg-blue-400">
      <div class="mx-auto max-w-7xl px-2 sm:px-6 lg:px-8">
        <div class="relative flex items-center justify-between h-16">
          <div class="flex-shrink-0">
            <a href="/" class="flex items-center">
              <span class="ml-2 text-gray-800 font-semibold text-lg">Blog</span>
            </a>
          </div>
          <div class="hidden sm:block">
            <div class="flex space-x-4">
              <a
                href="/post/all"
                class="text-gray-800 hover:bg-yellow-500 hover:text-white px-3 py-2 rounded-md text-sm font-medium"
                >Home</a
              >
              <!-- Display "Create Post" and "Manage Posts" only if the user is logged in -->
              <% if (isLoggedIn) { %>
              <a
                href="/post/create"
                class="text-gray-800 hover:bg-yellow-500 hover:text-white px-3 py-2 rounded-md text-sm font-medium"
                >Create Post</a
              >
              <a
                href="/post/myPosts"
                class="text-gray-800 hover:bg-yellow-500 hover:text-white px-3 py-2 rounded-md text-sm font-medium"
                >Manage Posts</a
              >
              <!-- Display "Manage Users" link if the user is an admin -->
              <% } %> <% if (isAdmin) { %>
              <a
                href="/user"
                class="text-gray-800 hover:bg-yellow-500 hover:text-white px-3 py-2 rounded-md text-sm font-medium"
                >Manage Users</a
              >

              <!-- Display "Logout" if the user is logged in, otherwise display "Login" -->
              <% } %> <% if (isLoggedIn) { %>
              <a
                href="/auth/logout"
                class="text-gray-800 hover:bg-yellow-500 hover:text-white px-3 py-2 rounded-md text-sm font-medium"
                >Logout</a
              >
              <% } else { %>
              <a
                href="/auth/login"
                class="text-gray-800 hover:bg-yellow-500 hover:text-white px-3 py-2 rounded-md text-sm font-medium"
                >Login</a
              >
              <% } %>
            </div>
          </div>
        </div>
      </div>
    </nav>

